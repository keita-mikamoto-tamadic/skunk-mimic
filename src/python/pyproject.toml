[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dora-rs==0.3.13",
    "pyarrow>=23.0.1",
]
[tool.uv.workspace]
members = ["dummy_input"]

