# Build artifacts
build/
*.o
*.a
*.so
*.dylib

# Test binaries (built locally)
src/cpp/lib/robot_config_test
src/cpp/node/robot_control_manager/test_sm

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Dora runtime output
out/
# daemon が dataflow yaml を持たない場合 (分散実行の受け側) のログ出力先。
# daemon の cwd/_work/<session-uuid>/out/<dataflow-uuid>/log_*.jsonl にセッション毎に増える。
# 中身が out/ だけなので上の行に隠れて git status に出てこない → 明示的に無視する。
_work/

# IDE / tools
.vscode/
.serena/
.claude/

# Python virtual environments
.venv/

# Reference / external repos (not part of this project)
ref/

# Logs
sysid/data/

# Python cache
__pycache__/
*.pyc

# dora 認証トークン (秘密。コミット厳禁)
.dora-token
