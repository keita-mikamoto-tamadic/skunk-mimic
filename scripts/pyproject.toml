[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["mujoco", "numpy"]

[tool.uv.workspace]
members = [
    "calc_lqr_gain",
    "calc_ab_matrix",
    "imu_noise_meas",
]
